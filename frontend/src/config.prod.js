const config = {
  API_URL: 'https://ethiopia-problem-solver-backend.onrender.com',
  APP_URL: 'https://your-frontend-url.netlify.app' // We'll update this after frontend deployment
};

export default config;